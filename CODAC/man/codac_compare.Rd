% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codac_compare.R
\name{codac_compare}
\alias{codac_compare}
\title{Execute the CODAC_Compare Analysis Pipeline}
\usage{
codac_compare(
  data,
  timepoints,
  groups,
  n_observations = 1,
  comparisons = NULL,
  targets_to_plot = NULL,
  r2_threshold = 0.4,
  p_threshold = 0.05,
  p_value_option = "FDR",
  p_value_comparison = "RAW",
  min_rhythmicity = "HIGH",
  rhythmicity_cutoff = "HIGH",
  amp_stringency = 0.5,
  interval_var = 1,
  missing_data_action = "KEEP",
  plot_flag = "N",
  plot_all = "N",
  exclude_medium = TRUE,
  time_label = "ZT"
)
}
\arguments{
\item{data}{A dataframe: target names in column 1, then the value columns
ordered by group (see above).}

\item{timepoints}{Numeric/character vector of collection times (e.g., c(2,6,10,14,18,22)).}

\item{groups}{Character vector naming the groups, IN THE SAME ORDER as the
value-column blocks in `data` (e.g., c("WT_chow","WT_cdHFD","KO_chow","KO_cdHFD")).}

\item{n_observations}{Integer. Number of replicates per timepoint.}

\item{comparisons}{Optional list of group pairs to compare, each a length-2
character vector, e.g. list(c("WT_chow","KO_chow"), c("WT_cdHFD","WT_chow")).
If NULL, all pairwise combinations are compared.}

\item{targets_to_plot}{Character vector. Specific targets to plot.}

\item{r2_threshold}{Minimum R-squared threshold (default: 0.4).}

\item{p_threshold}{Significance level for p-value (default: 0.05).}

\item{p_value_option}{Multiple-testing strategy for the PER-GROUP rhythmicity
test: 'FDR' or 'RAW' (default: 'FDR'). It can also be given as a
`c(method, alpha)` pair (e.g. `c('FDR', 0.1)`) to use its own threshold
instead of the shared `p_threshold`.}

\item{p_value_comparison}{Which p-values drive the PAIRWISE COMPARISON decisions
(Mesor_Change, the Cat 2/3 loss/gain confidence, and the Cat 4-7
amplitude/phase split): 'RAW' (raw pairwise p-values) or 'FDR'
(Benjamini-Hochberg adjusted). This is INDEPENDENT of `p_value_option`.
Both raw and FDR columns are always exported; only the decisions switch
(default: 'RAW'). It can also be given as a `c(method, alpha)` pair
(e.g. `c('FDR', 0.1)`) to use its own threshold.}

\item{min_rhythmicity}{Minimum rhythmicity tier (default: 'ARRHYTHMIC').}

\item{rhythmicity_cutoff}{Rhythmicity level at which a group counts as rhythmic
in the classification: 'ARRHYTHMIC','LOW','MEDIUM','HIGH','EXTREMELY HIGH' (default: 'HIGH').}

\item{amp_stringency}{Amplitude-filter strictness from 0 to 1: 0 = off, 0.5 = default (validated), 1 = strictest (default: 0.5).}

\item{interval_var}{Interval variance flexibility: 1, 2, or 3 (default: 1).}

\item{missing_data_action}{How to handle missing values: 'KEEP', 'IMPUTE', or 'REMOVE' (default: 'KEEP').}

\item{plot_flag}{Character. "Y" to generate individual plots (default: "N").}

\item{plot_all}{Character. "Y" to plot all, "N" to plot only targets_to_plot (default: "N").}

\item{exclude_medium}{Logical. If TRUE, drop targets classified as MEDIUM in any group before the comparisons (default: TRUE).}

\item{time_label}{X-axis label in plots (default: 'ZT').}
}
\value{
A dataframe with the per-target fits and pairwise comparison results.
  All p-value, delta and metric columns are returned as numeric (not text),
  so they can be filtered directly (e.g. `dplyr::filter(p_diff_amplitude < 0.05)`)
  without any `as.numeric()` conversion.
}
\description{
Wrapper for the CODAC_Compare Python engine. Unlike Single/Flex (which analyze
one target series at a time), CODAC_Compare works with MULTIPLE GROUPS: it fits
rhythmicity per group and performs pairwise differential-rhythm comparisons
between groups (e.g. WT vs KO).
}
\details{
The data columns must be ordered by group: for each group, all timepoints,
each with its replicates. So the number of value columns must equal
length(groups) * length(timepoints) * n_observations.
}
